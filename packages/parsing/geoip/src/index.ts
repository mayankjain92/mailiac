import type { ForensicHop } from '@mailiac/shared-types';

export async function enrichHopsWithGeo(hops: ForensicHop[]): Promise<ForensicHop[]> {
  void hops;
  throw new Error('TODO: implement enrichHopsWithGeo');
}
