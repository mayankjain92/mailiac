export function signPayload(payload: string, signingSecret: string, timestamp: number): string {
  void payload;
  void signingSecret;
  void timestamp;
  throw new Error('TODO: implement signPayload');
}
